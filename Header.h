#include <iostream>
#include <string>
using namespace std;
string StrMultInt(const string& num, int multiplier);
string StrPlusStr(const string& num1, const string& num2);
string PowStr(int base, int exponent);
int proverka();
